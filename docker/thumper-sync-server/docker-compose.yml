services:
  syncthing:
    image: syncthing/syncthing:latest
    restart: unless-stopped
    ports:
      - "8384:8384"
      - "22000:22000/tcp"
      - "22000:22000/udp"
      - "21027:21027/udp"
    volumes:
      - ./syncthing-config:/var/syncthing/config
      - /opt/connaissance/vault:/var/syncthing/vault
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=UTC
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8384"]
      interval: 30s
      timeout: 10s
      retries: 3

  git-watcher:
    image: alpine:latest
    restart: unless-stopped
    depends_on:
      syncthing:
        condition: service_healthy
    volumes:
      - /opt/connaissance:/opt/connaissance
      - /opt/connaissance/vault:/vault
      - ./git-watcher.sh:/git-watcher.sh:ro
      - /home/phaedrus/.ssh:/root/.ssh:ro
    env_file:
      - .env
    environment:
      - VAULT_PATH=/vault
      - GIT_AUTHOR_NAME=connaissance-watcher
      - GIT_AUTHOR_EMAIL=${GITHUB_EMAIL:-phaedrus@example.com}
      - GIT_COMMITTER_NAME=connaissance-watcher
      - GIT_COMMITTER_EMAIL=${GITHUB_EMAIL:-phaedrus@example.com}
    entrypoint: /bin/sh
    command:
      - -c
      - |
        apk add --no-cache git inotify-tools curl && \
        git config --global --add safe.directory /opt/connaissance && \
        cd /opt/connaissance && \
        sh /git-watcher.sh
    working_dir: /opt/connaissance
